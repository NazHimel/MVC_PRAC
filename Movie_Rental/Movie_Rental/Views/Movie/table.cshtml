

@model Movie_Rental.Models.Movie


@{
    ViewBag.Title = "table";
}

<h2>@(Model.Id)</h2>
@{
    for (int i = 1; i < 11; i++)
    {
        <h1>@(i) * @(Model.Id) =
            @(i* Model.Id)
        </h1>
    
    }
}

<h2></h2>

